name: Test Access Secrets

on:
    workflow_dispatch:
    pull_request:
    pull_request_review_comment:
    pull_request_review:
    issue_comment:

jobs:
    first:
        runs-on: ubuntu-latest
        steps:
            - name: Use secret
              run: echo "bbb"
              env:
                TOKEN: ${{ secrets.IMPORTANT }}
